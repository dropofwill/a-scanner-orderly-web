var socket;
socket = io.connect( window.location.origin, {query: 'bar', type: 'desktop'});

socket.on('new', data{
	var order = "";
	order += "<div class='order'>>."
	order += "<span class='label><spam"
});